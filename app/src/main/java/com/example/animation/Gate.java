package com.example.animation;

import android.graphics.Bitmap;

public class Gate extends Figure{
    public Gate(int x, int y, Bitmap bitmap, int w, int h) {
        super(x, y, bitmap, w, h);
    }
}
